using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Suffix : Affix
{
    public Suffix(string name, string description, List<StatModifier> mods) : base(name, description, mods)
    {
    }
}
